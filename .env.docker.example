# Environment variables for Docker container
# Copy this to .env and customize as needed

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Python Configuration
PYTHONUNBUFFERED=1

# Optional: Custom paths (if not using default)
# DATA_DIR=/app/data
# MODELS_DIR=/app/ml/models
# EXPORTS_DIR=/app/data/exports
